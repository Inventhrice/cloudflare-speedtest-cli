# Cloudflare Speedtest CLI

A quick bash function using cURL and Cloudflare's speedtest service to measure download speeds. By default, the script downloads 111MB of data.

This references the [speed-cloudflare-cli](https://github.com/KNawm/speed-cloudflare-cli/) repo.
